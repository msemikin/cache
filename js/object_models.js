/*Просто кусок кода. Бэкап. Не удаляйте, плиз.
        list[ind].remove();
        var delInd = objSt.indexOf(txt);
        if (delInd > -1) objSt.splice(delInd,1);
        objD.push(txt);
        //удаление
        var text = focused.el.textContent;
        objSt.push(text);
        getList();*/
